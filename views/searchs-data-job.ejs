<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Searchs Data Job</title>
    <style>
         body {
          font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
          margin: 20px;
          background-color: #f9fafb;
          color: #333;
        }

        a {
          color: #2563eb;
          text-decoration: none;
          font-weight: 500;
        }

        a:hover {
          text-decoration: underline;
        }

        .search-form {
          display: flex;
          gap: 8px;
          align-items: center;
          justify-content: flex-start;
        }

        .search-form input[type="text"] {
          padding: 8px 10px;
          border: 1px solid #d1d5db;
          border-radius: 6px;
          font-size: 0.9rem;
          min-width: 200px;
        }

        .search-form button {
          padding: 8px 10px;
          background: #10b981; /* เขียว */
          color: white;
          border: none;
          border-radius: 8px;
          font-size: 0.9rem;
          font-weight: 500;
          cursor: pointer;
          transition: background 0.2s, transform 0.2s;
        }

        .search-form button:hover {
          background: #059669;
          transform: translateY(-2px);
        }
        
        h4 {
          text-align: center; // margin: 20px 0;
          font-size: 1.25rem;
          font-weight: bold;
          color: #111827;
        }

        .table-container {
          overflow-x: auto;
          background: #fff;
          border-radius: 12px;
          box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
          padding: 1rem;
        }

        table {
          width: 100%;
          border-collapse: collapse;
          min-width: 600px;
        }

        thead {
          background-color: #2563eb;
          color: #fff;
        }

        thead th {
          padding: 12px 10px;
          text-align: center;
          font-size: 0.9rem;
          font-weight: 600;
        }

        tbody tr {
          border-bottom: 1px solid #e5e7eb;
          transition: background 0.2s;
        }

        tbody tr:hover {
          background-color: #f3f4f6;
        }

        tbody td {
          padding: 10px;
          font-size: 0.9rem;
          color: #374151;
          text-align: center;
        }

        tbody td:first-child {
          white-space: nowrap;
        }

        .btn-action {
          margin-right: 8px;
          padding: 4px 8px;
          border-radius: 6px;
          font-size: 0.8rem;
          font-weight: 500;
          transition: all 0.2s;
        }

        .btn-edit {
          background: #2563eb;
          color: white;
        }

        .btn-edit:hover {
          background: #1e40af;
        }

        .btn-delete {
          background: #dc2626;
          color: white;
        }

        .btn-delete:hover {
          background: #991b1b;
        }
    
        @media (max-width: 768px) {
          table {
            font-size: 0.8rem;
            min-width: unset;
          }
          thead {
            display: none;
          }
          tbody tr {
            display: block;
            margin-bottom: 1rem;
            border: 1px solid #e5e7eb;
            border-radius: 10px;
            padding: 10px;
            background: #fff;
          }
          tbody td {
            display: flex;
            justify-content: space-between;
            padding: 8px 5px;
          }
          tbody td::before {
            content: attr(data-label);
            font-weight: 600;
            color: #111827;
          }
        }

        .top-link {
          position: absolute;
          top: 20px;
          right: 20px;
          color: #007BFF;
          text-decoration: none;
          font-weight: bold;
        }

        .top-link:hover {
          text-decoration: underline;
        }
    </style>
</head>
<body>
    <a href="/index" class="top-link">หน้าแรก</a><br><br>
    <form method="get" action="/searchs-data-job" class="search-form">
        <% 
            let searchw = (searchj) ? searchj : '' 
        %>
        <input type="text" name="searchjob" placeholder="ค้นหา (งาน) ที่ต้องการ" class="search-form" value="<%= searchw %>" class="search-input">
        <button type="submit" class="search-btn">ค้นหา</button>
    </form>
    <h4>ค้นหาข้อมูลรับสมัครงาน</h4>
    <% if (locals.data) { %>
    <div class="table-container">
    <table border="1">
        <thead><tr><th>ชื่อบริษัท</th><th>สถานที่ตั้ง</th><th>เว็บไซต์</th><th>ตำแหน่งที่เปิดรับ</th><th>รายละเอียด</th><th>เงินเดือน</th><th>เบอร์ติดต่อ</th><th>วันที่เพิ่มข้อมูล</th></tr></thead>
        <% for (doc of data) {
            let dateTimeStamp = new Date(Date.parse(doc.date_added))
            let date = dateTimeStamp.getDate()
            let month = dateTimeStamp.getMonth() + 1
            let year = dateTimeStamp.getFullYear()
            let now = `${date}-${month}-${year}`
        %>
        <tbody>
        <tr>
            <td data-label="ชื่อบริษัท"><%= doc.companyname %></td>
            <td data-label="สถานที่ตั้ง"><%= doc.location %></td>
            <td data-label="เว็บไซต์"><%= doc.websitecompany %></td>
            <td data-label="ตำแหน่งที่เปิดรับ"><%= doc.title %></td>
            <td data-label="รายละเอียด"><%= doc.description %></td>
            <td data-label="เงินเดือน"><%= doc.salary %></td>
            <td data-label="เบอร์ติดต่อ"><%= doc.telephonenumber %></td>
            <td data-label="วันที่เพิ่มข้อมูล"><%= now %></td>
        </tr>
        </tbody>
        <% } %>
    </table>
    </div>
    <% } %>
</body>
</html>
