<!DOCTYPE html>
<html lang="th-US">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload File Page</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            flex-direction: column; /* จัดแนวบนลงล่าง */
            justify-content: center; /* center แนวตั้ง */
            align-items: center; /* center แนวนอน */
            font-family: Arial, sans-serif;
            background: #f4f4f9;
            text-align: center;
        }

        form {
            background: #fff;
            padding: 20px 30px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            max-width: 90%;
            width: 350px;
        }

        input[type="file"] {
            width: 100%;
        }

        input[type="checkbox"] {
            margin-right: 5px;
        }

        button {
            padding: 8px 16px;
            font-size: 16px;
            border: none;
            border-radius: 8px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        #result {
            margin-top: 20px;
            background: #fff;
            padding: 15px 20px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            max-width: 90%;
            width: 350px;
        }

        #result img {
            max-width: 100px;
            max-height: 100px;
            margin-bottom: 10px;
            border-radius: 8px;
        }

        a {
            text-decoration: none;
            color: #333;
            margin-top: 10px;
            display: inline-block;
        }

        a:hover {
            text-decoration: underline;
        }

        .top-link {
          color: #007BFF;
          text-decoration: none;
          font-weight: bold;
        }
    
        .top-link:hover {
          text-decoration: underline;
        }

        @media (max-width: 400px) {
            form, #result {
                width: 90%;
                padding: 15px;
            }

            button {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <br><br>
    <a href="/index" class="top-link">หน้าแรก</a>
    <form method="post" action="/upload" enctype="multipart/form-data">
        <input type="file" name="upfile" accept="image/*"><br><br>
        <input type="checkbox" name="overwrite" value="true">เขียนทับ (ถ้าชื่อซ้ำ)<br><br>
        <button type="submit">OK</button>
    </form>
    <br>
    <% if (locals.file) { %>
        <div id="result">
            <hr>
            <img src="<%= file %>" style="max-width: 120px; max-height: 120px;"><br>
            Name: <%= fileInfo.name %><br>
            Type: <%= fileInfo.type %><br>
            Size: <%= fileInfo.size %> bytes<br>
            Last Modified: <%= fileInfo.lastModified %>
        </div>
        <br><br>
        <a href="/index" class="top-link">หน้าแรก</a>
    <% } %>
</body>
</html>
