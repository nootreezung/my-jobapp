<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edits Job</title>
    <style>
        body {
          font-family: Arial, sans-serif;
          margin: 0;
          padding: 20px 20px 20px 20px;
          display: flex;
          justify-content: center;
          align-items: center;
          background: #f9f9f9;
        }
    
        form {
          position: relative;
          background: #fff;
          padding: 40px 20px 40px 20px;
          border-radius: 10px;
          box-shadow: 0 2px 8px rgba(0,0,0,0.1);
          max-width: 600px;
          width: 100%;
        }
    
        form h4 {
          margin-bottom: 15px;
          text-align: center;
          color: #333;
        }
    
        .form-group {
          display: flex;
          flex-direction: column;
          margin-bottom: 15px;
        }
    
        label {
          margin-bottom: 5px;
          font-weight: bold;
          color: #444;
        }
    
        input, textarea {
          padding: 10px;
          border: 1px solid #ccc;
          border-radius: 5px;
          font-size: 1rem;
          width: 100%;
          box-sizing: border-box;
        }
    
        textarea {
          resize: vertical;
        }
    
        button {
          background: #007BFF;
          color: white;
          padding: 12px;
          border: none;
          border-radius: 5px;
          font-size: 1rem;
          cursor: pointer;
          width: 100%;
        }
    
        button:hover {
          background: #0056b3;
        }
    
        .top-link {
          position: absolute;
          top: 10px;
          right: 15px;
          font-size: 0.9rem;
          color: #007BFF;
          text-decoration: none;
          font-weight: bold;
        }
    
        .top-link:hover {
          text-decoration: underline;
        }
    
        @media (max-width: 480px) {
          .top-link {
            position: static;
            display: block;
            text-align: right;
            margin-bottom: 10px;
          }
    
          form {
            padding-top: 20px;
          }
        }
    
        @media (min-width: 768px) {
          .form-group {
            flex-direction: row;
            align-items: center;
          }
    
          label {
            width: 150px;
            margin-bottom: 0;
          }
    
          input, textarea {
            flex: 1;
          }
        }
    </style>
</head>
<body>
    <% if (locals.data) { %>
    <div class="form-group">
    <form method="post" action="/edits-job/<%= data._id %>">
        <a href="/edits-and-deletes-data-job" class="top-link">ยกเลิก</a>
        <h4>แก้ไขข้อมูลรับสมัครงาน</h4>
        <label>ชื่อบริษัท</label>
        <input type="text" name="companyname" value="<%= data.companyname %>" placeholder="ชื่อบริษัท" required><br><br>
        <label>สถานที่ตั้งของบริษัท</label>
        <input type="text" name="location" value="<%= data.location %>" placeholder="สถานที่ตั้งของบริษัท" required><br><br>
        <label>เว็บไซต์ของบริษัท</label>
        <input type="text" name="websitecompany" value="<%= data.websitecompany %>" placeholder="เว็บไซต์ของบริษัท" required><br><br>
        <label>ตำแหน่งที่รับสมัคร</label>
        <input type="text" name="title" value="<%= data.title %>" placeholder="ตำแหน่งที่รับสมัคร" required><br><br>
        <label>รายละเอียดของการรับสมัคร</label>
        <textarea name="description" cols="60" rows="5" placeholder="รายละเอียดของการรับสมัคร"><%= data.description %></textarea><br><br>
        <label>เรตเงินเดือน (กำหนดเป็นช่วง)</label>
        <input type="text" name="salary" value="<%= data.salary %>" placeholder="เรตเงินเดือน (กำหนดเป็นช่วง)" required><br><br>
        <label>เบอร์ติดต่อ</label>
        <input type="text" name="telephonenumber" value="<%= data.telephonenumber %>" placeholder="เบอร์ติดต่อ" required><br><br>
        <%
            // แปลงวันที่แบบ String ที่ได้จาก MongoDB ให้เป็น Timestamp สำหรับแสดงผล
            // โดยแสดงในรูปแบบของปี/เดือน/วัน
            let dateString = new Date(Date.parse(data.date_added))
            let month = dateString.getMonth() + 1
            month = (month > 10) ? month : '0' + month
            let date = dateString.getDate()
            date = (date > 10) ? date : '0' + date
            let year = dateString.getFullYear()
            let dateTimeStamp = `${year}-${month}-${date}`
        %>
        <label>วันที่ลงประกาศ</label>
        <input type="date" name="date_added" value="<%= dateTimeStamp %>" placeholder="วันที่ลงประกาศ" required><br><br>
        <button type="submit">ส่งข้อมูล</button>
    </form>
    </div>
    <% } %>
</body>
</html>
